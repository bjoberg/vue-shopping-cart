const state = {
  totalItems: 0
};

const getters = {
  getShoppingCartTotalItems: (state) => state.totalItems
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
}